
import java.util.Scanner;

public class OOPS {

    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);  // Scanner is a predefined class in java and we are creating an object from the scanner class so that through the same we can access methods of Scanner class eg. nextInt(), nextFloat() and so on.
         // but now we have a created a class (Rectangle)

        // Creating object from Rectangle class.

        Rectangle1 rect=new Rectangle1();  // Object Created.

//        rect.width=10;   // set the value of data field with the help of object using dot operator.
//        rect.length=20;
//
//        rect.perimeter(rect.width, rect.length);
//        rect.area(rect.width, rect.length);
 //         rect.showName();

          int a=10,b=20,c=300,d=1000,e=5;


          // format specifiers %d for integers %f for float %s for string and %c for characters

          //System.out.printf("The value of a is %d, b is %d c is %d, d is %d and e is %d", a,b,c,d,e );
          //System.out.format("The value of a is %d, b is %d c is %d, d is %d and e is %d", a,b,c,d,e);


         {
             char ch='#';
         }

          char ch='$';














    }


}
